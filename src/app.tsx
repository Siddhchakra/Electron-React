import * as React from 'react';
import * as ReactDOM from 'react-dom';
import LoginComponent from './client/components/login/login';

ReactDOM.render(<LoginComponent />, document.querySelector('.container'));